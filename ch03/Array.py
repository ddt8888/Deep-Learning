# # 1차원 배열
# import numpy as np

# a = np.array([1, 2, 3, 4])
# print(a)
# print(np.ndim(a))   # 배열의 차원수를 구하는 ndim()함수
# print(a.shape)      # 배열의 형상을 반환하는 shape
# print(a.shape[0])

# 2차원 배열
import numpy as np

a = np.array([[1, 2], [3, 4], [5, 6]])

print(a)
print(np.ndim(a))   # 배열의 차원수를 구하는 ndim()함수
print(a.shape)      # 배열의 형상을 반환하는 shape
